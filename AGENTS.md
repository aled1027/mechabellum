# Agent Notes

This repo’s roadmap lives in `IMPLEMENATATION_PLAN.md` (intentional filename) and the design reference is `GDD.md`.
Use those two documents as the source of truth for implementation details and requirements.
